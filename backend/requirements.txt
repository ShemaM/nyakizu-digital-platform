django==5.2.10
djangorestframework==3.16.0
django-cors-headers==4.7.0
python-decouple==3.8
django-allauth==65.9.0
django-unfold==0.78.1
Pillow==10.4.0

# Required by django-allauth Google OAuth provider
requests>=2.34
PyJWT>=2.8
cryptography>=41.0

# Production: WSGI server, Postgres driver, DATABASE_URL parsing, static file serving
gunicorn==23.0.0
psycopg2-binary==2.9.10
dj-database-url==2.3.0
whitenoise==6.8.2

# Production: persistent media storage (avatars, product photos) — S3-compatible,
# targets Cloudflare R2. Inactive unless R2_BUCKET_NAME is set (see settings.py).
django-storages==1.14.6
boto3==1.43.66

# Error monitoring — inactive unless SENTRY_DSN is set (see settings.py).
sentry-sdk==2.66.1

# Transactional email over HTTPS (Resend) — inactive unless RESEND_API_KEY
# is set (see settings.py). Raw SMTP is blocked outbound on Render's free tier.
django-anymail==15.1

# Web Push notifications — inactive unless VAPID_PRIVATE_KEY is set (see settings.py).
pywebpush==2.0.3
